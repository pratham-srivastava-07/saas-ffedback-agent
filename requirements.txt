# Top-level dependencies. Previously a full pip freeze, which made it
# impossible to tell what the project actually depends on.

# --- web ---
fastapi>=0.116
uvicorn[standard]>=0.35
pydantic>=2.11
pydantic-settings>=2.4
# Required by FastAPI for multipart form uploads (POST /analyze/csv).
python-multipart>=0.0.9

# --- graph + models ---
langgraph>=0.5.1
langchain>=0.3.26
langchain-core>=0.3.68
langchain-groq>=0.3.6
langchain-google-genai>=2.0.10

# --- clustering ---
numpy>=1.26
scikit-learn>=1.5

# --- storage ---
SQLAlchemy>=2.0.41
aiosqlite>=0.20
greenlet>=3.0

# --- misc ---
python-dotenv>=1.1
